# Family Tree Authentication
# Set to 'development' to disable password protection for local development
NODE_ENV=development

# Password hash for production (generated using bcrypt)
# Use the generatePasswordHashForSetup function in auth.ts to generate this
FAMILY_TREE_PASSWORD_HASH=

# GitHub repository for family data (for CI/CD)
FAMILY_TREE_DATA_REPO=familytree-data

# Optional: Additional password hashes for multiple users
# FAMILY_TREE_PASSWORD_HASH_2=